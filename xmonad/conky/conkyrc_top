# conkyrc file for top dzen bar
###

background no
out_to_console yes
update_interval 1

TEXT
${cpu cpu0} ${freq_g} ${mem} ${memmax} ${memperc} ${fs_type} ${fs_used} ${fs_size} ${fs_used_perc} \
${time %a %b %d %H:%M}
